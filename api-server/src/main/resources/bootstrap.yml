spring.application.name: api-server

spring:
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
